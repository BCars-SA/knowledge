/**
 * @fileoverview Request schemas and types for API endpoints
 * Includes Zod validation schemas for input validation
 */

import { z } from "zod";

/**
 * Schenma for health check request
 */
export const HealthRequestSchema = z.object({});
